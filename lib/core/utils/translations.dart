

import 'package:flutter/material.dart';

class Translations {
  static const localesPath = 'assets/translations';
  static const List<Locale> supportedLocales = [Locale('en', 'US'), Locale('zh', 'CN')];
}

